{
  "name": "socket-call-server",
  "version": "0.10.0",
  "type": "module",
  "files": [
    "/index.js",
    "/index.d.ts",
    "/zod.js",
    "/zod.d.ts"
  ],
  "main": "index.js",
  "types": "index.d.ts",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc",
    "prepack": "tsc",
    "test": "pnpm test:types && bun test",
    "test:types": "tsc --noEmit -p tests/tsconfig.json"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "typescript": "^7.0.2",
    "zod": "^4.5.4"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "peerDependenciesMeta": {
    "zod": {
      "optional": true
    }
  }
}
