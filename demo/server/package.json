{
    "name": "socket-call-server-demo",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "bun run --watch index.ts"
    },
    "dependencies": {
        "socket.io": "^4.7.0",
        "socket-call-server": "*"
    }
}